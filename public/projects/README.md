Place real project thumbnails here, e.g.:

- chess-game-api.jpg
- crud-api.jpg

Referenced from `lib/data.ts` (`image` field on each project). Until real
screenshots are added, `ProjectCard` renders a placeholder pattern instead of
these paths.
